<?php

//oneclick importer
function ocdi_import_files() {
	return array(
		array(
			'import_file_name'           => 'One Page Slider',
			'import_file_url'            => plugins_url( '/demo-data/onepageslider.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/one-slider.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon/',
		),
		array(
			'import_file_name'           => 'One Page Video',
			'import_file_url'            => plugins_url( '/demo-data/onepagevideo.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/one-video.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon2/',
		),
		array(
			'import_file_name'           => 'One Page Youtube',
			'import_file_url'            => plugins_url( '/demo-data/onepageyoutube.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/one-youtube.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon3/',
		),
		array(
			'import_file_name'           => 'Dark One Page Slider',
			'import_file_url'            => plugins_url( '/demo-data/dark-onepageyoutube.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/dark-one-slider.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon4/',
		),
		array(
			'import_file_name'           => 'Dark One Page Video',
			'import_file_url'            => plugins_url( '/demo-data/dark-onepagevideo.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/dark-one-video.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon5/',
		),
		array(
			'import_file_name'           => 'Dark One Page Youtube',
			'import_file_url'            => plugins_url( '/demo-data/dark-onepageyoutube.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/dark-one-youtube.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon6/',
		),
		array(
			'import_file_name'           => 'Agency Multi Page 1',
			'import_file_url'            => plugins_url( '/demo-data/multiagency-1.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/agency-one.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon7/',
		),
		array(
			'import_file_name'           => 'Agency Multi Page 2',
			'import_file_url'            => plugins_url( '/demo-data/multiagency-2.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/agency-two.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon8/',
		),
		array(
			'import_file_name'           => 'Clean Personal Multi Page',
			'import_file_url'            => plugins_url( '/demo-data/multipersonal.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/personal.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon9/',
		),
		array(
			'import_file_name'           => 'Dark Personal Multi Page',
			'import_file_url'            => plugins_url( '/demo-data/dark-multipersonal.xml' , __FILE__ ),
			'import_widget_file_url'     => plugins_url( '/demo-data/cordon.wie' , __FILE__ ),
			'import_preview_image_url'   => plugins_url( '/demo-data/personal-dark.jpg' , __FILE__ ),
			'import_notice'                => __( '<p>To prevent any error, please use the clean wordpress site to import the demo data. </p><p>Or you can use this plugin 
			<a href="https://wordpress.org/plugins/wordpress-database-reset/" target="_blank">WordPress Database Reset</a> to reset/clear the database first.</p><p>After you import this demo, you will have to setup the page builder,google map & flickr settings separately.</p>', 'cordon_plg' ),
			'preview_url'                => 'http://theme.ridianur.com/cordon10/',
		),
	);
}
add_filter( 'pt-ocdi/import_files', 'ocdi_import_files' );


add_filter( 'pt-ocdi/disable_pt_branding', '__return_true' );


function ocdi_after_import( $selected_import ) {
	
	// Assign menus to their locations.
    $one_page = get_term_by( 'name', 'one-page-menu', 'nav_menu' );
    $multi_page = get_term_by( 'name', 'multipage-menu', 'nav_menu' );
    set_theme_mod( 'nav_menu_locations', array(
			'ridianur-homepage-menu' => $multi_page->term_id,
            'ridianur-onepage-menu' => $one_page->term_id,	
        )
    );

	if ( 'One Page Slider' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/onepageslider.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Slider' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'One Page Video' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/onepagevideo.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Video' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'One Page Youtube' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/onepageyoutube.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Youtube' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Dark One Page Slider' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/dark-onepageslider.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Slider' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Dark One Page Video' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/dark-onepagevideo.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Video' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Dark One Page Youtube' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/dark-onepageyoutube.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Onepage Youtube' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Agency Multi Page 1' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/multiagency-1.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Home' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Agency Multi Page 2' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/multiagency-2.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Home' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Clean Personal Multi Page' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/multipersonal.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Home' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}
	if ( 'Dark Personal Multi Page' === $selected_import['import_file_name'] ) {
 		// The exported theme options string
        $str = file_get_contents( plugins_url( '/demo-data/dark-multipersonal.txt' , __FILE__ ));
        $theme_options = unserialize(ot_decode($str));
        update_option(ot_options_id(), $theme_options);
		
		// Assign front page and posts page (blog page).
		$front_page_id = get_page_by_title( 'Home' );
	
		update_option( 'show_on_front', 'page' );
		update_option( 'page_on_front', $front_page_id->ID );

	}

}
add_action( 'pt-ocdi/after_import', 'ocdi_after_import' );

